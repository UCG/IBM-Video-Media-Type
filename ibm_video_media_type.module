<?php

/**
 * @file
 * Hook implementations for the IBM video media type module.
 *
 * @todo Implement thumbnail stuff:
 *   1) Add "thumbnail entity reference ID" "field" to source field JSON in
 *   IbmVideo.
 *   2) Add code in IbmVideo to grab local thumbnail URI using this field (the
 *   thumbnail URI is generated from a hash of the ID/base embed URL), and
 *   download a thumbnail if necessary to this URI.
 *   3) Adjust the method for generating the source field value to generate a
 *   thumbnail entity reference ID if one has not been generated.
 *   4) Adjust widget/formatter to work with above changes.
 *   5) Adjust constraint validator to work with this.
 *   6) Remove unnecessary methods.
 * @todo Review.
 * @todo Test.
 */

declare (strict_types = 1);

/**
 * Implements hook_theme().
 */
function ibm_video_media_type_theme($existing, $type, $theme, $path) {
  return [
    'ibm_video_player' => [
      'variables' => ['videoUrl' => ''],
      'template' => 'ibm_video_player',
    ],
  ];
}
