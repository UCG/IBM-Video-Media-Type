<?php

/**
 * @file
 * Hook implementations for the IBM video media type module.
 *
 * @todo Figure out what to do about thumbnails.
 * @todo Review.
 * @todo Test.
 * @todo Decide if restrictions on channel / channel video ID are too tight.
 */

declare (strict_types = 1);

/**
 * Implements hook_theme().
 */
function ibm_video_media_type_theme($existing, $type, $theme, $path) {
  return [
    'ibm_video_player' => [
      'variables' => ['videoUrl' => ''],
      'template' => 'ibm_video_player',
    ],
  ];
}
