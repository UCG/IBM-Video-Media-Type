<?php

/**
 * @file
 * Hook implementations for the IBM video media type module.
 *
 * @todo Switch over to using non-permalink URLs and gathering embed URL +
 * video ID.
 * @todo Implement thumbnail support.
 * @todo Review.
 * @todo Test.
 */

declare (strict_types = 1);

/**
 * Implements hook_theme().
 */
function ibm_video_media_type_theme($existing, $type, $theme, $path) {
  return [
    'ibm_video_player' => [
      'variables' => ['videoUrl' => ''],
      'template' => 'ibm_video_player',
    ],
  ];
}
