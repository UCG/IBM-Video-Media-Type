<?php

/**
 * @file
 * Hook implementations for the IBM Video Media Type module.
 *
 * @todo Implement thumbnail stuff in IbmVideo:
 *   - Add code to grab local thumbnail URI (the thumbnail URI is generated
 *   from a hash of the ID/base embed URL), and download a thumbnail if
 *   necessary to this URI.
 * @todo Adjust widget to work with recent changes.
 * @todo Adjust formatter '' '' '' '' ''.
 * @todo Adjust constraint validation '' '' '' '' ''.
 * @todo Review.
 * @todo Test.
 */

declare (strict_types = 1);

/**
 * Implements hook_theme().
 */
function ibm_video_media_type_theme($existing, $type, $theme, $path) {
  return [
    'ibm_video_player' => [
      'variables' => ['videoUrl' => ''],
      'template' => 'ibm_video_player',
    ],
  ];
}
